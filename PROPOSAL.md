# 2026 MoonBit 黑客松项目申报说明书

## 一、 项目基本信息

- **项目名称**：MoonPLC —— 软 PLC 运行时与 IEC 61131-3 子集编译器
- **开源仓库**：[https://github.com/lycvvt/Moonplc](https://github.com/lycvvt/Moonplc)
- **申报赛道**：语言与开发工具（解释器、运行时、编译器/DSL）/ 系统与网络基础设施
- **开源许可证**：Apache-2.0
- **开发语言**：纯 MoonBit（Pure MoonBit）
- **交付形式**：MoonBit 核心库 + CLI 终端仿真器 + WebAssembly 浏览器工业交互工作台

---

## 二、 选题背景与解决的真实问题

### 1. 行业痛点：工业软件与工控核心受制于人
在工业自动化与智能制造领域，可编程逻辑控制器（PLC）是工业装备的“大脑”。然而，传统的软 PLC 运行时（如德国 CODESYS、倍福 TwinCAT）长期被海外商业软件垄断且高度闭源；开源方案（如 MATIEC、OpenPLC）大多采用古老的 C/Python 代码库，代码庞大臃肿，容易出现内存安全缺陷，且无法良好适配现代 WebAssembly 与边缘容器环境。

### 2. 教学与研发痛点：工业仿真环境笨重
高校自动化、电气工程专业学生及工控算法工程师在学习调试 IEC 61131-3 标准语言时，通常必须安装数十 GB 的庞大商业工控 IDE。业界极度缺乏一个**“零依赖、纯原生、打开浏览器即可秒级运行调试”**的轻量级软 PLC 教学与仿真实验环境。

### 3. MoonPLC 的破局定位
**MoonPLC** 借助 MoonBit 现代语言的**内存安全、强静态类型、极小 Wasm 打包体积与无 GC/轻量实时**等核心特性，用纯 MoonBit 从零实现了 IEC 61131-3 工业标准的编程语言子集（结构化文本 ST + 梯形图 LD）以及严格遵循毫秒级扫描周期（Scan Cycle）的软 PLC 执行引擎，为工控边缘计算与 Web 工业仿真提供了现代化的开源基石。

---

## 三、 核心架构与技术创新

```
                            MoonPLC 系统全景架构
┌────────────────────────────────────────────────────────────────────────┐
│                        前端：IEC 61131-3 语言子集                       │
│                                                                        │
│   ┌───────────────────────────┐       ┌────────────────────────────┐   │
│   │ 结构化文本 (ST) 源码文本   │       │   梯形图 (LD) 梯级数据结构  │   │
│   └─────────────┬─────────────┘       └─────────────┬──────────────┘   │
│                 │ (Lexer & Parser)                  │ (LD -> AST 转换) │
│                 ▼                                   ▼                  │
│   ┌────────────────────────────────────────────────────────────────┐   │
│   │               AST (抽象语法树) 与 符号表 (SymbolTable)          │   │
│   └───────────────────────────────┬────────────────────────────────┘   │
└───────────────────────────────────┼────────────────────────────────────┘
                                    │ 解释执行 / 调度
                                    ▼
┌────────────────────────────────────────────────────────────────────────┐
│                        核心：MoonPLC 执行引擎                          │
│                                                                        │
│   ┌──────────────────┐   ┌─────────────────────┐   ┌───────────────┐   │
│   │  表达式求值引擎  │   │  控制流解释器 (AST) │   │ 标准功能块库  │   │
│   │  (Expr Evaluator)│   │  (IF/WHILE/FOR)     │   │ (TON/TOF/CTU) │   │
│   └─────────┬────────┘   └──────────┬──────────┘   └───────┬───────┘   │
│             └───────────────────────┼──────────────────────┘           │
│                                     ▼                                  │
│   ┌────────────────────────────────────────────────────────────────┐   │
│   │                  工业标准三段式扫描周期调度器                  │   │
│   │   [1. 输入采样]  ──>  [2. 执行程序]  ──>  [3. 刷新输出]        │   │
│   └───────────────────────────────┬────────────────────────────────┘   │
│                                   │                                    │
│   ┌───────────────────────────────┴────────────────────────────────┐   │
│   │               PLC 工业过程映像区 (Process Image)               │   │
│   │     %IX (数字输入) / %QX (数字输出) / %MW (内部标志寄存器)     │   │
│   └────────────────────────────────────────────────────────────────┘   │
└───────────────────────────────────┬────────────────────────────────────┘
                                    │
                                    ▼
┌────────────────────────────────────────────────────────────────────────┐
│                          外设仿真与交付应用                            │
│                                                                        │
│   ┌──────────────────────────────┐    ┌────────────────────────────┐   │
│   │  CLI 仿真器 (终端多周期交互) │    │ Web/Wasm 工业孪生工作台    │   │
│   │  - 载入 .st 案例自动化单步   │    │ - 电机启保停自锁电路旋转   │   │
│   │  - 虚拟 I/O 状态实时监控     │    │ - 十字路口红绿灯定时控制   │   │
│   └──────────────────────────────┘    └────────────────────────────┘   │
└────────────────────────────────────────────────────────────────────────┘
```

### 技术创新点：
1. **纯 MoonBit 实现的 IEC 61131-3 编译前端**：
   - 实现了工业标准的词法分析器（支持大小写不敏感关键字、时间字面量如 `T#5s`、`TIME#100ms` 以及直接物理地址 `%IX0.0`、`%QX0.1`）。
   - 实现了基于算符优先级的递归下降语法分析器（支持算术、关系比较、短路逻辑表达式以及 IF-THEN-ELSIF-ELSE、WHILE、FOR、功能块调用）。
2. **梯形图（LD）多维网络到 AST 的等价编译引擎**：
   - 将常开（NO）、常闭（NC）触点和输出线圈（Normal、Set、Reset）结构化建模，支持复杂的串联（AND）与并联（OR）布尔网络，并无缝编译为统一 AST。
3. **确定性扫描周期（Deterministic Scan Cycle）执行引擎**：
   - 严格还原工业 PLC 的“输入采样 → 程序执行 → 输出刷新”三段式运行机制。
   - 内置工业核心标准功能块：`TON`（通电延时定时器）、`TOF`（断电延时定时器）、`TP`（脉冲定时器）、`CTU`（增计数器）。
4. **数字孪生仿真界面（Web Studio）**：
   - 借助 MoonBit 极速轻量的优势，提供浏览器开箱即用的工业仿真体验，包含实时动态梯形图导通点亮、虚拟 PLC I/O 机架 LED 状态灯、以及电机旋转和交通信号灯动效。

---

## 四、 本期项目交付清单

| 交付模块 | 路径 | 功能说明 | 状态 |
| :--- | :--- | :--- | :---: |
| **基础类型与地址** | `types.mbt`, `types_test.mbt` | IEC 基础数据类型与 `%IX0.0` 地址编解码 | 已完成，单测 100% 通过 |
| **AST 语法树** | `ast.mbt` | 表达式、语句、功能块与程序组织单元树结构 | 已完成，编译通过 |
| **ST 词法分析器** | `lexer.mbt`, `lexer_test.mbt` | Token 切分、时间字面量、注释过滤 | 已完成，单测 100% 通过 |
| **ST 语法分析器** | `parser.mbt`, `parser_test.mbt` | 递归下降语法解析、控制流与表达式构建 | 已完成，单测 100% 通过 |
| **梯形图编译器** | `ladder.mbt`, `ladder_test.mbt` | 梯级 Rung、触点与线圈布尔代数转 AST 引擎 | 已完成，单测 100% 通过 |
| **过程映像存储区** | `memory.mbt`, `memory_test.mbt` | %I / %Q / %M 寄存器映像与物理地址符号映射 | 已完成，单测 100% 通过 |
| **标准功能块** | `fblocks.mbt`, `fblocks_test.mbt` | TON、TOF、TP 定时器与 CTU 计数器 | 已完成，单测 100% 通过 |
| **周期调度引擎** | `runtime.mbt`, `runtime_test.mbt` | 三段式扫描调度、表达式求值器、时钟步进 | 已完成，单测 100% 通过 |
| **统一 Facade API**| `moonplc.mbt`, `moonplc_test.mbt` | 一键加载 ST/LD、I/O 控制与周期执行 | 已完成，单测 100% 通过 |
| **CLI 命令行程序** | `cmd/main/main.mbt` | 电机启保停自锁与红绿灯控制终端演示 | 已完成，运行验证通过 |
| **Web 仿真工作台** | `web/index.html` | 现代化 SCADA 工业数字孪生控制台 | 已完成，交互验证通过 |
| **标准工业案例** | `examples/*.st`, `*.json` | 启保停电机、智能交通灯、流水线推杆 | 已完成 |
| **持续集成 CI** | `.github/workflows/ci.yml` | 自动化代码检查与全套测试构建 | 已配置 |

---

## 五、 复现与验证方式

```bash
# 1. 克隆代码仓库
git clone https://github.com/lycvvt/Moonplc.git
cd Moonplc

# 2. 静态检查与格式验证
moon fmt
moon check

# 3. 运行全套自动化测试 (覆盖所有核心模块)
moon test

# 4. 运行终端工业场景仿真演示
moon run cmd/main

# 5. 打开浏览器运行可视化工业数字孪生工作台
# 直接在浏览器中打开 web/index.html 即可交互操作
```
