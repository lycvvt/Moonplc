# 2026 MoonBit 黑客松项目申报说明书

## 一、 项目基本信息

- **项目名称**：MoonPLC —— 软 PLC 运行时与 IEC 61131-3 子集编译器
- **开源仓库**：[https://github.com/lycvvt/Moonplc](https://github.com/lycvvt/Moonplc)
- **申报赛道**：语言与开发工具（解释器、运行时、编译器/DSL）/ 系统与网络基础设施
- **开源许可证**：Apache-2.0
- **开发语言**：纯 MoonBit（Pure MoonBit）
- **交付形式**：MoonBit 核心库 + CLI 终端仿真器 + WebAssembly 浏览器工业交互工作台

---

## 二、 选题背景与解决的真实问题

### 1. 行业痛点：工业软件与工控核心受制于人
在工业自动化与智能制造领域，可编程逻辑控制器（PLC）是工业装备的“大脑”。然而，传统的软 PLC 运行时（如德国 CODESYS、倍福 TwinCAT）长期被海外商业软件垄断且高度闭源；开源方案（如 MATIEC、OpenPLC）大多采用古老的 C/Python 代码库，代码庞大臃肿，容易出现内存安全缺陷，且无法良好适配现代 WebAssembly 与边缘容器环境。

### 2. 教学与研发痛点：工业仿真环境笨重
高校自动化、电气工程专业学生及工控算法工程师在学习调试 IEC 61131-3 标准语言时，通常必须安装数十 GB 的庞大商业工控 IDE。业界极度缺乏一个**“零依赖、纯原生、打开浏览器即可秒级运行调试”**的轻量级软 PLC 教学与仿真实验环境。

### 3. MoonPLC 的破局定位
**MoonPLC** 借助 MoonBit 现代语言的**内存安全、强静态类型、极小 Wasm 打包体积与无 GC/轻量实时**等核心特性，用纯 MoonBit 从零实现了 IEC 61131-3 工业标准的编程语言子集（结构化文本 ST + 梯形图 LD）以及严格遵循毫秒级扫描周期（Scan Cycle）的软 PLC 执行引擎，并进一步拓展了工业闭环 PID 控制器、Modbus 现场总线协议栈与 IL 字节码指令虚拟机，为工控边缘计算与 Web 工业仿真提供了现代化的开源基石。

---

## 三、 核心架构与技术创新

```
                            MoonPLC 系统全景架构
┌────────────────────────────────────────────────────────────────────────┐
│                        前端：IEC 61131-3 语言子集                       │
│                                                                        │
│   ┌───────────────────────────┐       ┌────────────────────────────┐   │
│   │ 结构化文本 (ST) 源码文本   │       │   梯形图 (LD) 梯级数据结构  │   │
│   └─────────────┬─────────────┘       └─────────────┬──────────────┘   │
│                 │ (Lexer & Parser)                  │ (LD -> AST 转换) │
│                 ▼                                   ▼                  │
│   ┌────────────────────────────────────────────────────────────────┐   │
│   │               AST (抽象语法树) 与 符号表 (SymbolTable)          │   │
│   └───────────────────────────────┬────────────────────────────────┘   │
└───────────────────────────────────┼────────────────────────────────────┘
                                    │ 解释执行 / IL 编译
                                    ▼
┌────────────────────────────────────────────────────────────────────────┐
│                   核心：MoonPLC 执行引擎与工业扩展                     │
│                                                                        │
│   ┌──────────────────┐   ┌─────────────────────┐   ┌───────────────┐   │
│   │  表达式求值引擎  │   │  控制流解释器 (AST) │   │ 标准功能块库  │   │
│   │  (Expr Evaluator)│   │  (IF/WHILE/FOR)     │   │ (TON/TOF/CTU) │   │
│   └─────────┬────────┘   └──────────┬──────────┘   └───────┬───────┘   │
│             ├───────────────────────┼──────────────────────┤           │
│             ▼                       ▼                      ▼           │
│   ┌──────────────────┐   ┌─────────────────────┐   ┌───────────────┐   │
│   │  IL 字节码虚拟机 │   │  工业闭环 PID 控制器│   │ Modbus 协议栈 │   │
│   │  (PlcVm Bytecode)│   │  (抗饱和/无扰切换)  │   │ (01/02/03/05) │   │
│   └─────────┬────────┘   └──────────┬──────────┘   └───────┬───────┘   │
│             └───────────────────────┼──────────────────────┘           │
│                                     ▼                                  │
│   ┌────────────────────────────────────────────────────────────────┐   │
│   │                  工业标准三段式扫描周期调度器                  │   │
│   │   [1. 输入采样]  ──>  [2. 执行程序]  ──>  [3. 刷新输出]        │   │
│   └───────────────────────────────┬────────────────────────────────┘   │
│                                   │                                    │
│   ┌───────────────────────────────┴────────────────────────────────┐   │
│   │               PLC 工业过程映像区 (Process Image)               │   │
│   │     %IX (数字输入) / %QX (数字输出) / %MW (内部标志寄存器)     │   │
│   └────────────────────────────────────────────────────────────────┘   │
└───────────────────────────────────┬────────────────────────────────────┘
                                    │
                                    ▼
┌────────────────────────────────────────────────────────────────────────┐
│                          外设仿真与交付应用                            │
│                                                                        │
│   ┌──────────────────────────────┐    ┌────────────────────────────┐   │
│   │  CLI 仿真器 (终端多周期交互) │    │ Web/Wasm 工业孪生工作台    │   │
│   │  - 电机自锁起保停实机时序    │    │ - 动态导通发光梯形图       │   │
│   │  - 交通路口红绿灯定时控制    │    │ - 虚拟 PLC 机架发光指示灯  │   │
│   │  - 恒温水箱 PID 阶跃收敛     │    │ - 三相交流电机 2800RPM     │   │
│   │  - Modbus 寄存器遥测读写     │    │ - 道路交通灯物理动态仿真   │   │
│   └──────────────────────────────┘    └────────────────────────────┘   │
└────────────────────────────────────────────────────────────────────────┘
```

### 技术创新点：
1. **纯 MoonBit 实现的 IEC 61131-3 完整子集编译体系**：
   - 词法解析器支持大小写无关匹配、时间字面量（`T#5s`）与直接物理地址（`%IX0.0`）。
   - 语法解析器基于算符优先级爬升与递归下降，支持表达式、控制流与 POU 块。
   - 梯形图（LD）高级编译器，支持常开、常闭、比较触点（`CMP >= 50`）、边沿触点与输出线圈到 AST 的无缝降维映射。
2. **工业闭环控制与协议栈扩展**：
   - 内置工业级连续 PID 控制器，包含抗积分饱和（Anti-windup）、死区滤波、微分先行与手/自动无扰切换（Bumpless Transfer）。
   - 内置 Modbus 现场总线协议处理层（FC 01, 02, 03, 05, 06），实现工业寄存器与上位机 SCADA 的无缝映射。
   - 内置底层指令表（IL）字节码虚拟机（PlcVm），支持累加器操作与条件跳转执行。
3. **确定性扫描周期（Deterministic Scan Cycle）调度**：
   - 还原输入采样、程序执行、输出刷新三段式循环，配合 32 项自动化测试保证 100% 绿色可靠。
4. **零依赖开箱即用 WebAssembly 数字孪生**：
   - 双击打开 `web/index.html` 即可进入专业 SCADA 工业界面，完成毫秒级控制仿真。

---

## 四、 本期项目交付清单

| 交付模块 | 源码文件 | 功能说明 | 状态 |
| :--- | :--- | :--- | :---: |
| **基础类型与地址** | `types.mbt`, `types_test.mbt` | IEC 基础数据类型与 `%IX0.0` 地址编解码 | 已完成，100% PASS |
| **AST 语法树** | `ast.mbt` | 表达式、语句、功能块与程序组织单元树结构 | 已完成，编译通过 |
| **ST 词法分析器** | `lexer.mbt`, `lexer_test.mbt` | Token 切分、时间字面量、注释过滤 | 已完成，100% PASS |
| **ST 语法分析器** | `parser.mbt`, `parser_test.mbt` | 递归下降语法解析、控制流与表达式构建 | 已完成，100% PASS |
| **梯形图高级编译器** | `ladder.mbt`, `ladder_test.mbt` | 触点（常开/常闭/比较/边沿）与线圈转 AST | 已完成，100% PASS |
| **过程映像存储区** | `memory.mbt`, `memory_test.mbt` | %I / %Q / %M 寄存器映像与物理地址符号映射 | 已完成，100% PASS |
| **标准功能块** | `fblocks.mbt`, `fblocks_test.mbt` | TON、TOF、TP、TONR、CTU、CTUD、SR、RS、R_TRIG、F_TRIG | 已完成，100% PASS |
| **闭环 PID 控制器** | `pid.mbt`, `pid_test.mbt` | 工业 PID 控制器、抗积分饱和、水温恒温仿真 | 已完成，100% PASS |
| **Modbus 协议栈** | `modbus.mbt`, `modbus_test.mbt` | Modbus 请求响应编解码与寄存器映射 (FC 1/2/3/5/6) | 已完成，100% PASS |
| **IL 字节码虚拟机** | `vm.mbt`, `vm_test.mbt` | 指令表虚拟机、累加器逻辑、算术运算与跳转 | 已完成，100% PASS |
| **周期调度引擎** | `runtime.mbt`, `runtime_test.mbt` | 三段式扫描调度、表达式求值器、时钟步进 | 已完成，100% PASS |
| **统一 Facade API**| `moonplc.mbt`, `moonplc_test.mbt` | 一键加载 ST/LD、I/O 控制与周期执行 | 已完成，100% PASS |
| **CLI 命令行程序** | `cmd/main/main.mbt` | 电机启保停、红绿灯、PID 调节、Modbus 4 大场景终端演示 | 已完成，运行验证通过 |
| **Web 仿真工作台** | `web/index.html` | 现代化 SCADA 工业数字孪生控制台 | 已完成，交互验证通过 |
| **标准工业案例** | `examples/*.st`, `*.json` | 启保停电机、智能交通灯、流水线推杆、恒温水箱 | 已完成 |
| **持续集成 CI** | `.github/workflows/ci.yml` | 自动化 CI 持续集成构建流水线 | 已配置 |

---

## 五、 复现与验证方式

```bash
# 1. 克隆代码仓库
git clone https://github.com/lycvvt/Moonplc.git
cd Moonplc

# 2. 静态检查与格式验证
moon fmt
moon check

# 3. 运行全套 32 项自动化测试
moon test

# 4. 运行终端 4 大工业场景仿真演示
moon run cmd/main

# 5. 打开浏览器运行可视化工业数字孪生工作台
# 直接在浏览器中双击打开 web/index.html 即可交互操作
```
